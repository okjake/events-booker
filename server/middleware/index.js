const { validationCancelReg, loginValidation } = require('./validation');
const { checkEmailIfExist } = require('./checkEmail');

module.exports = { validationCancelReg, loginValidation, checkEmailIfExist };
