const checkEmail = require('./checkEmail');
const signAttendanceSql = require('./signAttendanceSql');
const getAdminData = require('./getAdmins');

module.exports = {
  checkEmail,
  signAttendanceSql,
  getAdminData,
};
